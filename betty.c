#include <stdio.h>

/**
 * main - calculates the sum of two numbers.
 *
 * Return: 0 on success.
 */

int main(void)
{
	int num1 = 5, num2 = 5, sum = 0;

	sum = num1 + num2;

	printf("%d is the sum.", sum);

	return (0);
}
